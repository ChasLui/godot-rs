[package]
name = "itest"
description = "Integration tests run inside a real Godot 4 instance."
version = "0.1.0"
license = "MIT"
workspace = ".."
edition = "2021"
rust-version = "1.70"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
godot = { path = "../godot", version = "0.1.0" }
