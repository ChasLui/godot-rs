[package]
name = "counter"
description = "Properties, signals and frame-driven async in one Godot 4 extension."
version = "0.1.0"
license = "MIT"
edition = "2021"
rust-version = "1.70"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
godot = { path = "../../godot", version = "0.1.0" }
